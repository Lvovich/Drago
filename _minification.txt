install:
    download zip from http://dl.google.com/closure-compiler/compiler-latest.zip
    extract compiler.jar
Use:
    java -jar compiler.jar --charset UTF-8 --js source_code/drago.js --js source_code/drago_proto_mousedown.js --js source_code/drago_proto_mousemove.js --js source_code/drago_proto_mouseup.js --js source_code/drago_proto_lib.js --js_output_file min/drago.min.js --compilation_level ADVANCED_OPTIMIZATIONS


gulp (only concatenation to one file, for dev debugging):
install:
    npm install --save-dev gulp gulp-concat
Use:
    gulp